# Dependencies for the optional CtrlRegen pixel-removal backend
# (mertizci/noai-watermark). The backend is cloned at a pinned commit by
# setup_ctrlregen.sh and imported at runtime; it is never bundled.
#
# torch is installed separately in setup_ctrlregen.sh with the correct
# platform index (CUDA or CPU), so it is intentionally not listed here.
#
# Hugging Face pins are the Dependabot High floors (or the smallest
# compatible companion pins those floors require). Validate against the
# pinned noai-watermark commit before treating a CtrlRegen runtime as
# verified. This file is only ever installed inside the dedicated venv
# created by setup_ctrlregen.sh (service profile "ctrlregen"), never into
# the main service image.
#
# High floors: transformers>=5.10.0, diffusers>=0.38.0.
# Companion pins: huggingface_hub>=1.5.0 (transformers 5.10.x) and
# tokenizers>=0.22,<0.23.1 (transformers 5.10.x; there is no 0.23.0).
# accelerate stays 1.14.0 (Medium GHSA, no High first_patched_version).
# Pillow stays 12.3.0 (not in the High pip set).
diffusers==0.40.0
transformers==5.17.0
tokenizers==0.22.2
accelerate==1.15.0
controlnet-aux==0.0.10
color-matcher==0.6.0
safetensors==0.8.0
huggingface_hub==1.27.0
Pillow==12.3.0
piexif==1.1.3
